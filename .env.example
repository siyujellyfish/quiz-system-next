# Database runtime
# Local Docker PostgreSQL uses postgres-js because @neondatabase/serverless
# only connects through Neon's remote proxy.
DATABASE_DRIVER=postgres-js
DATABASE_URL="postgres://user:password@host:port/db-name"

# Neon / Vercel Marketplace:
# - omit DATABASE_DRIVER so runtime defaults to @neondatabase/serverless
# - DATABASE_URL is the pooled application connection
# - DATABASE_URL_UNPOOLED is preferred by Drizzle Kit migrations
DATABASE_URL_UNPOOLED="postgres://user:password@host:port/db-name"

DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=change-me-please

# Codex on Vercel Sandbox
# Vercel deployments authenticate @vercel/sandbox automatically through OIDC.
# Pin a tested Codex CLI version for production. If omitted, the sandbox
# bootstrap currently falls back to latest.
CODEX_VERSION=""

# Optional custom developer instructions for the question AI tutor.
CODEX_CHAT_DEVELOPER_INSTRUCTIONS=""

# Local development only: if VERCEL_OIDC_TOKEN is not available, provide all
# three values below. Do not expose these as PUBLIC_* variables.
VERCEL_TOKEN=""
VERCEL_TEAM_ID=""
VERCEL_PROJECT_ID=""
